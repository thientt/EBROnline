@model EBROnline.ViewModel.AccountViewModel
@{
    ViewBag.Title = "Edit Profile";
}

<link rel="stylesheet" href="@Url.Content("~/Content/bootstrap-theme.min.css")" />
<link rel="stylesheet" href="@Url.Content("~/Content/bootstrap.min.css")" />

<br />
<br />

@if (ViewData["Result"] == "SUCESS")
{
    <div class="alert alert-success fade in">
        <a href="#" class="close" data-dismiss="alert">&times;</a>
        <strong>Success!</strong>Edit Profile successfully
    </div>
}
@if (ViewData["Result"] == "FAILURE")
{
    <div class="alert alert-danger fade in">
        <a href="#" class="close" data-dismiss="alert">&times;</a>
        <strong>Error!</strong>Edit Profile failure
    </div>
}

@using (@Html.BeginForm())
{
    <div class="row">
        <div class="col-xs-12 col-md-offset-2 col-md-8">
            <div class="panel panel-default">
                <div class="panel-heading">
                    <div class="panel-title">
                        <strong>Edit Profile</strong>
                    </div>
                </div>
                <div class="panel-body">
                    <div class="form-horizontal">
                        <div class="form-group">
                            @Html.HiddenFor(x => x.Id)
                            <label class="control-label col-lg-2">First Name</label>
                            <div class="col-lg-10">
                                @Html.TextBoxFor(model => model.FirstName, new { @class = "form-control" })
                                @Html.ValidationMessageFor(x => x.FirstName)
                            </div>
                        </div>
                        <div class="form-group">
                            <label class="control-label col-lg-2"> Last Name </label>
                            <div class="col-lg-10">
                                @Html.TextBoxFor(model => model.LastName, new { @class = "form-control" })
                                @Html.ValidationMessageFor(x => x.LastName)
                            </div>
                        </div>
                        <div class="form-group">
                            <label class="control-label col-lg-2">Register Date </label>
                            <div class="col-lg-10">
                                @Html.EBRTextBoxFor(model => model.RegisterDate, htmlAttributes: new { @Value = @Model.RegisterDate.ToString("d"), @class = "form-control" }, disabled: true)
                                @Html.HiddenFor(x => x.RegisterDate)
                            </div>
                        </div>
                        <div class="form-group">
                            <label class="control-label col-lg-2">Email</label>
                            <div class="col-lg-10">
                                @Html.EBRTextBoxFor(model => model.Email, new { @class = "form-control" }, disabled: true)
                                @Html.HiddenFor(x => x.Email)
                            </div>
                        </div>
                        <div class="form-group">
                            <label class="control-label col-lg-2">Role </label>
                            <div class="col-lg-10">
                                @Html.EBRTextBoxFor(model => model.RoleName, new { @class = "form-control" }, disabled: true)
                                @Html.HiddenFor(x => x.RoleId)
                                @Html.HiddenFor(x => x.RoleName)
                            </div>
                        </div>
                        <div class="form-group">
                            <label class="control-label col-lg-2">Phone</label>
                            <div class="col-lg-10">
                                @Html.TextBoxFor(x => x.Phone, htmlAttributes: new { @class = "form-control" })
                            </div>
                        </div>
                        <div class="form-group">
                            <div class="col-lg-offset-2 col-lg-10">
                                <button class="btn btn-primary btn-width-md" type="submit" value="Save" name="btnSubmit" id="btnSubmit">Save</button>
                            </div>
                        </div>
                    </div>
                </div>
            </div>
        </div>
    </div>
}

<div class="row">
    <div class="col-xs-12 col-md-offset-2 col-md-8">
        @Html.ActionLink("My Account", "Index") |
        @Html.ActionLink("Change Password", "ChangePassword", new { @id = Model.Id })
    </div>
</div>

@Html.Partial("_PartialPageLoading")
